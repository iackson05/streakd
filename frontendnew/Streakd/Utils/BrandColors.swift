import SwiftUI

extension Color {
    static let brand = Color(red: 1.0, green: 0.42, blue: 0.21) // #FF6B35
    static let cardBackground = Color.white.opacity(0.05)
    static let cardBorder = Color.white.opacity(0.1)
    static let subtleText = Color.white.opacity(0.4)
    static let bodyText = Color.white.opacity(0.7)
    static let dimText = Color.white.opacity(0.3)
}
